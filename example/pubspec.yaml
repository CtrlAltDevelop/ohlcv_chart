name: ohlcv_chart_example
description: Demonstrates the ohlcv_chart candlestick and depth charts.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  ohlcv_chart:
    path: ../

dev_dependencies:
  flutter_lints: ">=5.0.0 <7.0.0"
  image: ">=4.0.0 <5.0.0"
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
