# ohlcv_chart example

Run it with:

```bash
flutter run
```

The demo generates a synthetic random-walk market, then lets you toggle the
main-chart overlays and sub-chart indicators, switch between candle and line
mode, place trend, horizontal and vertical lines, and view the matching depth
chart.
